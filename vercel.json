{
  "buildCommand": "pnpm -w run build:tokens && cd packages/primitives && npx tsup --no-dts && cd ../.. && pnpm --filter ds0-docs run build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
