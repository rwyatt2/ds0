// Card is presentational — no behavior hook needed.
// This file exists for consistency with the component anatomy pattern.

/**
 * Card has no behavior to encapsulate.
 * It is a purely presentational compound component.
 * Each sub-component is a styled wrapper around HTML elements.
 */
export { };
