/**
 * React Native Button tests.
 *
 * These tests require @testing-library/react-native which is not yet installed.
 * Uncomment and run once the dependency is added:
 *
 * pnpm add -D @testing-library/react-native
 */

// import { render, screen, fireEvent } from '@testing-library/react-native';
// import { Button } from './Button';

describe('Button (Native)', () => {
    it.todo('renders with default props');
    it.todo('renders children text');
    it.todo('has accessibilityRole="button"');
    it.todo('has correct accessibilityState when disabled');
    it.todo('has correct accessibilityState when loading');
    it.todo('calls onPress when pressed');
    it.todo('does not call onPress when disabled');
});
