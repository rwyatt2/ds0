class DS0Stack extends HTMLElement { static get observedAttributes() { return ['direction', 'gap']; } constructor() { super(); this.attachShadow({ mode: 'open' }); } connectedCallback() { this.render(); } attributeChangedCallback() { this.render(); } private render() { if (!this.shadowRoot) return; const dir = this.getAttribute('direction') ?? 'vertical'; const gap = this.getAttribute('gap') ?? '16'; this.shadowRoot.innerHTML = `<style>:host{display:flex;flex-direction:${dir === 'horizontal' ? 'row' : 'column'};gap:${gap}px;}</style><slot></slot>`; } }
customElements.define('ds0-stack', DS0Stack); export { DS0Stack };
