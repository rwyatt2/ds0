class DS0Grid extends HTMLElement { static get observedAttributes() { return ['columns', 'gap']; } constructor() { super(); this.attachShadow({ mode: 'open' }); } connectedCallback() { this.render(); } attributeChangedCallback() { this.render(); } private render() { if (!this.shadowRoot) return; const cols = this.getAttribute('columns') ?? '1'; const gap = this.getAttribute('gap') ?? '16'; this.shadowRoot.innerHTML = `<style>:host{display:grid;grid-template-columns:repeat(${cols},1fr);gap:${gap}px;}</style><slot></slot>`; } }
customElements.define('ds0-grid', DS0Grid); export { DS0Grid };
