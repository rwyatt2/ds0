class DS0Calendar extends HTMLElement { constructor() { super(); this.attachShadow({ mode: 'open' }); } connectedCallback() { this.render(); } private render() { const now = new Date(); const m = now.getMonth(); const y = now.getFullYear(); const first = new Date(y,m,1).getDay(); const dim = new Date(y,m+1,0).getDate(); const today = now.getDate(); let cells = ''; for (let i = 0; i < first; i++) cells += '<div class="c e"></div>'; for (let d = 1; d <= dim; d++) cells += `<div class="c${d===today?' t':''}">${d}</div>`; this.shadowRoot!.innerHTML = `<style>:host{display:block}.h{display:flex;justify-content:space-between;align-items:center;margin-bottom:8px;font-size:14px;font-weight:600}.g{display:grid;grid-template-columns:repeat(7,1fr);gap:2px;text-align:center}.c{padding:8px;border-radius:6px;cursor:pointer;font-size:14px}.c:hover{background:#f1f5f9}.t{background:#2563eb;color:white;font-weight:600}.e{}</style><div class="h"><span>${now.toLocaleString('default',{month:'long',year:'numeric'})}</span></div><div class="g">${['Su','Mo','Tu','We','Th','Fr','Sa'].map(d=>`<div style="font-size:12px;color:#94a3b8;padding:4px">${d}</div>`).join('')}${cells}</div>`; this.setAttribute('role','grid'); } }
customElements.define('ds0-calendar', DS0Calendar);
export { DS0Calendar };
